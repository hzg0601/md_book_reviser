"""
1. 低像素图片超分辨率；
2. 英文说明文字翻译为中文；
3. 图中含有Figure 1\Algorithm 1等字眼的图片处理；

一、低像素图片超分辨率
（1）. 读取chapter路径下的所有图片；
（2）. 计算每张图片的分辨率，如果像素值小于阈值（如300像素），则将其放入一个列表中；
（3）. 针对列表中的低分辨率图片，进行超分辨率处理：
    （3.1）. 使用超分辨率算法（如ESRGAN等）对每张低分辨率图片进行处理，生成高分辨率版本；
    （3.2）. 使用Lanczos 插值算法对每张低分辨率图片进行处理，生成高分辨率版本；
（4）. 将处理后的高分辨率图片保存回原定目录下，命名方式添加super-resolution-'超分辨率算法'后缀以区分；


二、英文说明文字翻译为中文
（1）. 读取chapter路径下的所有图片；
（2）. 使用OCR技术（调用VLM模型）对每张图片进行文本识别，提取其中的文字内容；
（3）. 如果存在“Figure 1. xxx”类似的说明文字，将其翻译为中文，并替换原有的英文说明文字；
（4）. 将处理后的图片保存回原定目录下，命名方式添加notes-translation后缀以区分；

三、图中含有Figure 1\Algorithm 1\图1\算法1等字眼的图片处理
（1）. 读取chapter路径下的所有图片；
（2）. 使用OCR技术（调用VLM模型）对每张图片进行文本识别，提取其中的文字内容；
（3）. 遍历每张图片，判断其中是否存在Figure 1\Algorithm 1\图1\算法1等字眼，如果存在，则将其放入一个列表中；
（4）. 针对列表中的图片，删除图片中的Figure 1\Algorithm 1\图1\算法1等字眼；
（5）. 将处理后的图片保存回原定目录下，命名方式添加index-remove后缀以区分；

"""